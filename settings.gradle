pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'NeoForged'
            url = 'https://maven.neoforged.net/releases'
        }
    }
}

// An installed JDK 21 is required; no implicit JDK download during the port.
rootProject.name = 'ClangingHowl'
